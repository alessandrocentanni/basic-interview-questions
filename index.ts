// Create a new function that, give in input a string, returns the number of times each character appears in the string.
// For example, given "hello", the function should return {h: 1, e: 1, l: 2, o: 1}
export function countCharacters(str: string): { [key: string]: number } {
  // Your code here
  return { h: 1, e: 1, l: 2, o: 1 };
}

// Create a new function that, given in input a list of non negative integers, arranges them such that they form the largest possible number.
// For example, given [50, 2, 1, 9], the largest formed number is 95021.
export function largestNumber(arr: number[]): number {
  // Your code here
  return 95021;
}

// Calculate the moves you have to make to get to exactly 200.
// On each move you can either double your current amount or add another dollar.
export function solveDeduction(target = 500, addValue = 1): number {
  // Your code here
  return 13;
}
